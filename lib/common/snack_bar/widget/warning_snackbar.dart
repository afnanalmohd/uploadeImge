import 'package:flutter/material.dart';

class WarningSnackBar extends StatelessWidget {
  final String message;

  const WarningSnackBar({Key? key, required this.message}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
