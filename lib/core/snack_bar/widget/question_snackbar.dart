import 'package:flutter/material.dart';

class QuestionSnackBar extends StatelessWidget {
  final String message;

  const QuestionSnackBar({Key? key, required this.message}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
