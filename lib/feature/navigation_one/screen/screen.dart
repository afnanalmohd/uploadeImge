import 'package:flutter/cupertino.dart';

class NavigationOne extends StatelessWidget {
  const NavigationOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:  Text('Example1'));
  }
}
