import 'package:flutter/cupertino.dart';

class NavigationTwo extends StatelessWidget {
  const NavigationTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:  Text('Example2'));
  }
}
